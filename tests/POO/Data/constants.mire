// POO Data: constants are module-level immutable bindings

cons MAX_STOCK = 10

@[test]
pub fn constant_max_stock_readable: () :bool {
    set max_val = MAX_STOCK

    // Tests constant MAX_STOCK can be read into a variable
    return max_val == 10
}